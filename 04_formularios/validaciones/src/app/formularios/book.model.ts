import { Country } from '../formularios/country.model';

export class Book {
    author: string;
    title: string;
    editorial: string;
    exlibris: boolean;
    pages: number;
    genero: number;
    country: Country;
}

